# Example secrets file for Streamlit Cloud
# DO NOT commit the real secrets.toml — it is gitignored
# On Streamlit Cloud, set these in the "Secrets" dashboard section

# (Optional) Pre-fill the API key for private/internal deployments
# GEMINI_API_KEY = "AIzaSy..."
